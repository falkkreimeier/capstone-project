import React from 'react'
import OrderingInfo from './OrderingInfo'

export default {
  title: 'component/OrderingInfo',
  component: OrderingInfo,
}

const Template = args => <OrderingInfo />

export const Standard = Template.bind({})
